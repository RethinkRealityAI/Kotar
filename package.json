{
  "name": "kotar-estimates",
  "version": "1.0.0",
  "private": true,
  "description": "Kotar Renovations estimate builder - Netlify static site + functions",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod",
    "guide:shots": "node tools/guide-screenshots.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^20.0.0",
    "playwright-core": "^1.50.1"
  }
}
