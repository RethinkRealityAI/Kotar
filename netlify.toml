[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Client-facing estimate links: /e/<token>
[[redirects]]
  from = "/e/*"
  to = "/estimate.html"
  status = 200

# Client portal: /c/<token> lists every estimate sent to that client
[[redirects]]
  from = "/c/*"
  to = "/portal.html"
  status = 200

# How-to guide
[[redirects]]
  from = "/guide"
  to = "/guide.html"
  status = 200

# Friendly API paths -> functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
